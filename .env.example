# Supabase project configuration (public keys only — never commit service-role keys)
# When these are unset the app runs in local demo mode with sample household data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
